# include <stdio.h>
# include <stdbool.c>
void searchelement(int matrix[100][100],int rows,int cols,int target)
{
    int i=0,=cols-1;
    while(i<=rows && j>=0)
    {
        if(matrix[i][j]==target)
        {
            printf("element %D found at position (%d,%d)/n",target,i,j);
            return ;
        }

    }
}
